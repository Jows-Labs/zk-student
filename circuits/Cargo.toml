[workspace]
members = ["types", "program", "prover"]
resolver = "2"

[workspace.dependencies]
borsh = { version = "1", default-features = false, features = ["derive"] }
serde = { version = "1", features = ["derive"] }
sha2 = { version = "0.10", default-features = false }
